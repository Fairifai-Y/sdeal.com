# Beveiliging voor AdWords Tool
# Alleen toegankelijk voor specifieke IP-adressen

# Sta alleen jouw IP toe (vervang door jouw echte IP)
# Order Deny,Allow
# Deny from all
# Allow from 127.0.0.1
# Allow from ::1
# Allow from YOUR_IP_ADDRESS_HERE

# Basis authenticatie
AuthType Basic
AuthName "SDeal AdWords Tool"
AuthUserFile .htpasswd
Require valid-user

# Voorkom directory listing
Options -Indexes

# Beveilig tegen hotlinking
RewriteEngine On
RewriteCond %{HTTP_REFERER} !^$
RewriteCond %{HTTP_REFERER} !^https?://(www\.)?sdeal\.com [NC]
RewriteRule \.(html|css|js|png|jpg|gif)$ - [F]

# Cache control
<Files "*.html">
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires 0
</Files>
